# Fn

A fn is a procedure. A function is declared like this:

```
<visibility>? fn <name>(parameters) (-> <Return Type>)? {}
```

And each parameter looks like this

```
<name>: <Type>
```

where type is an ident
